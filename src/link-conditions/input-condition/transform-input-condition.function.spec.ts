import { transformInputCondition } from './transform-input-condition.function';

describe('transformInputCondition', () => {
  it('is a function', () => expect(typeof transformInputCondition).toBe('function'));
});
