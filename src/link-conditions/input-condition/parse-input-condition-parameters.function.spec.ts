import { parseInputConditionParameters } from './parse-input-condition-parameters.function';

describe('parseInputConditionParameters', () => {
  it('is a function', () => expect(typeof parseInputConditionParameters).toBe('function'));
});
