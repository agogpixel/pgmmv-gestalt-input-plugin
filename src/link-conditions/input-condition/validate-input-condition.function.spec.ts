import { validateInputCondition } from './validate-input-condition.function';

describe('validateInputCondition', () => {
  it('is a function', () => expect(typeof validateInputCondition).toBe('function'));
});
