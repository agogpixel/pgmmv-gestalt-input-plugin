import { createGestaltInputPlugin } from './create-gestalt-input-plugin.function';

describe('createGestaltInputPlugin', () => {
  it('is a function', () => expect(typeof createGestaltInputPlugin).toBe('function'));
});
